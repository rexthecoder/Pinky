{
  "name": "pinky",
  "version": "1.0.0",
  "description": "Flutter github action allows to auto submit apk or ipa file to various platform",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "prepare": "ncc build src/index.js --license licenses.txt -o src/dist --source-map"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "eslint": "^8.30.0",
    "form-data": "^4.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.31.1",
    "jest": "^27.2.5"
  }
}
